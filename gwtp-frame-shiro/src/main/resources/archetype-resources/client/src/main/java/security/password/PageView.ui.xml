<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
	xmlns:g='urn:import:com.google.gwt.user.client.ui'
	xmlns:m='urn:import:${package}.security.ui'>

	<ui:style gss="true" src="style.gss" type="${package}.security.password.PageView.MyStyle" />
	
	<g:HTMLPanel styleName='{style.containerStyle}'>
		<m:TitlePanel ui:field='titlePanel' title='密码' />
			<div class="{style.infoLists}">
				<div class="{style.mainFlow}">
					<div class="{style.mainNote}">
						<p>请选择安全系数高的密码。</p>
						<p>更改密码将会使您退出自己的设备。您重新登录时需要输入新密码。</p>
					</div>
				</div>
				<div class="{style.inputFrame}">
					<div class="{style.inputArea}">
						<c-wiz>
							<div class="{style.inputAreaWiz}">
								<div class="{style.inputControl}">
									<div class="{style.inputLevel0}">
										<div class="{style.inputLevel1}">
											<div class="{style.inputWithEyes}">
												<g:HTMLPanel styleName="{style.inputWrapHeight}" ui:field="inputwrap1" />
											</div>
										</div>
									</div>
									<div class="{style.inputControlPad}" ><div ui:field="controlpad1" /></div>
								</div>
								<div class="{style.commitTips}">
									<div class="{style.commitTipsTitle}">
										<span class="{style.commitTipsTitleContent}">密码强度</span>
									</div>
									<div class="{style.commitTipsContent}">不要使用很容易被猜到的密码，也不要使用您用于登录其他网站的密码。</div>
								</div>
								<div class="{style.inputControl}">
									<div class="{style.inputLevel0}">
										<div class="{style.inputLevel1}">
											<div class="{style.inputWithEyes}">
												<g:HTMLPanel styleName="{style.inputWrapHeight}" ui:field="inputwrap2" />
											</div>
										</div>
									</div>
									<div class="{style.inputControlPad}" ><div ui:field="controlpad2" /></div>
								</div>
							</div>
						</c-wiz>
					</div>
					<div class="{style.inputCommit}">
						<g:HTMLPanel styleName="{style.commitButton}" ui:field="commitButton">
							<content class="{style.buttonContent}">
								<span class="{style.buttonText}">更改密码</span>
							</content>
						</g:HTMLPanel>
					</div>
				</div>
			</div>
	</g:HTMLPanel>
</ui:UiBinder>